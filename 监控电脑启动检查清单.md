# 监控电脑启动检查清单

**日期**: 2026-01-06
**目的**: 确保监控电脑运行最新代码（包含 P2-2 冰山信号记录功能）

---

## ✅ 第一步：更新代码

在监控电脑上打开命令行，执行：

```bash
cd D:\onedrive\文档\ProjectS\flow-radar
git pull
```

**预期输出**:
```
Updating 43c7b58..eec9e29
Fast-forward
 SESSION_LOG_2026-01-06.md              | 418 +++++++++++++++++
 docs/P3-2_multi_signal_design.md       | 1375 +++++++++++++++++++++++++
 2 files changed, 1793 insertions(+)
```

---

## ✅ 第二步：验证代码版本

```bash
git log --oneline -3
```

**必须看到**:
```
eec9e29 P3-2 设计文档修正 (v1.1) + 代码审查日志  ← 最新！
43c7b58 Fix StateSaver for Windows: use replace() instead of rename()
b93eb11 Flow Radar: Complete module integration and 72h validation prep
```

如果第一行不是 `eec9e29`，说明更新失败！

---

## ✅ 第三步：验证关键功能存在

```bash
grep -n "def log_iceberg" core/event_logger.py
```

**必须看到**:
```
110:    def log_iceberg(self, iceberg: Dict, timestamp: float = None):
```

---

## ✅ 第四步：启动程序

```bash
start_alert_DOGE.bat
```

---

## ✅ 第五步：验证新功能生效（启动后 1 分钟内观察）

### 1. 检查事件日志文件正在写入
```bash
dir /O:D storage\events
```

应该看到今天的文件在不断更新。

### 2. 观察控制台输出

**应该能看到（如果有新的冰山信号）**:
```
✅ 记录冰山信号: DOGE/USDT BUY @ 0.150800
```

**如果看到节流提示（说明 P2-3 生效）**:
```
🔕 告警节流: iceberg:DOGE/USDT:BUY:ACTIVITY...
```

### 3. 等待 5 分钟后检查事件日志

```bash
# 解压查看最新事件
python -c "import gzip; f=gzip.open('storage/events/DOGE_USDT_2026-01-06.jsonl.gz','rt'); lines=f.readlines(); print(lines[-1])"
```

**如果看到 `"type": "iceberg"`，说明功能正常！**

---

## ⚠️ 故障排查

### 如果 git pull 失败
```bash
# 查看状态
git status

# 如果有冲突，暂存本地修改
git stash

# 再次拉取
git pull

# 恢复本地修改
git stash pop
```

### 如果启动失败
```bash
# 检查是否有程序占用
tasklist | findstr python

# 结束旧进程
taskkill /F /IM python.exe
```

---

## 📊 预期结果

更新并启动后，接下来 33 小时的数据将包含：
- ✅ 完整的冰山信号详情（价格、置信度、补单次数）
- ✅ 可用于 N=30 人工标注
- ✅ 可计算准确率

---

**准备好了随时告诉我，我帮你远程确认！** 🚀
